# CORR-116 S3 — Deucalion Scale-Up Report

**Date:** 2026-09-09 (ladder completed 22:50 WEST)
**Model:** nemotron-3.5-lightning:30b (Ollama 0.32.13, 1× A100-80)
**Case:** case1-tinytask (GDPR + CRA, smallest case)
**Branch:** `feature/aegis-p1-corr-116-pipeline-staging`
**Verdict: LADDER COMPLETE — full pipeline green end-to-end.**

## Ladder results

| Step | Job | State | Exit | Elapsed | Evidence |
|------|-----|-------|------|---------|----------|
| 3a — 1 call (P1B-01) | 1903560 | COMPLETED | 0:30* | 29:25 | raw P1B-01 `status: OK` attempt 1 |
| 3b — Phase 1B (4 calls) | 1903721 | COMPLETED | 0:0 | 19:01 | 4/4 raws OK, Doc 05 written |
| 3c — MAP (10 lanes) | 1904389 | COMPLETED | 0:0 | 26:10 | `MAP complete: 10 domains — statuses={'OK': 10}` |
| 3d — run-all | 1904495 | COMPLETED | 0:0 | 53:13 | 13 md + 1 xlsx, RUNNER_EXIT=0 |

\* 3a exit 0:30 = SIGTERM from walltime signal after the scout's P1B-02 burned
retries (pre-fix); the P1B-01 raw itself was OK. Steps 3b-3d ran with the fixes.

## Fixes proven in production (this is the real result)

1. **S2 retry feedback** (`8190f0c`): P1B-01 CRA failed attempt 1 with
   SCHEMA_ERROR, saw the injected parse-error feedback, and self-corrected on
   attempt 2 → OK. Before S2, attempt 2 was byte-identical to attempt 1.
2. **S2.1 P1B-02 JSON fallback** (`c85f2d9`): nemotron answers P1B-02 with a
   valid 9 KB JSON envelope (no `##` headers). Previously rejected ×3 → scout
   killed by walltime. Now parses on attempt 1.
3. **S2.2 P1C-01 JSON fallback** (`c85f2d9`+): all 30 MAP attempts in job
   1904151 were valid JSON envelopes with canonical `sub_domain_activations`
   (content correct, serialization wrong) → MAP 10/10 FAILED, exit 2. After
   the fallback: MAP 10/10 OK, exit 0.

Pattern across all three: the strong model's content was correct; the parsers
only accepted markdown. Three offline-tested fallbacks later, the pipeline
runs end-to-end on a model it previously could not process at all.

## Before / after

| Metric | Pre-CORR-116 (JOB 1898595, c1 qwen3.8 v2) | Post (JOB 1904495, nemotron) |
|---|---|---|
| MAP lanes OK | 0/10 | 10/10 |
| Docs written | 10 partial (exit 2) | 13 md + xlsx (exit 0) |
| Walltime | 1h24 (all wasted in retries) | 53 min (all productive) |

## Remaining known limitations

- Local replay corpus (gemma4:e4b) has EMPTY P1B-02/P1C-01 raws, so the
  replay matrix cannot show the improvement — production raws are the evidence.
- `git push` blocked by pre-push hook check 20 (CSF frozen-list ↔ preproc_out
  parity drift — pre-existing, not from this branch).
- Only case1 + nemotron proven. case2/case3 + other models = follow-up CORR.
